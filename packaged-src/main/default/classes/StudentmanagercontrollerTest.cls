@isTest
private class StudentmanagercontrollerTest {
    @isTest
    static void testDummy() {
        // Class is empty, dummy test to yield coverage state
        Studentmanagercontroller controller = new Studentmanagercontroller();
        System.assertNotEquals(null, controller, 'Instance should be created');
    }
}