// privacyPolicy.js
import { LightningElement } from 'lwc';

export default class PrivacyPolicy extends LightningElement {
    // No specific JavaScript logic is needed for this static content component.
    // The content is rendered directly from the HTML template.
}